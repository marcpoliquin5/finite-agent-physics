# Exact direct and transitive runtime constraints for the default API container.
# The base image is separately pinned by OCI digest in Dockerfile.
click==8.4.2
defusedxml==0.7.1
h11==0.16.0
pip==26.1.2
PyYAML==6.0.3
uvicorn==0.51.0
