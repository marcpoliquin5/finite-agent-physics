# Three-minute proof sequence

Target runtime: **2:50-2:58**. Record against the exact submitted tag. Every number on screen
must come from the generated evidence bundle or a clearly labeled live receipt.

## 0:00-0:18 — The missing control plane

**Visual:** ordinary agent DAG, then deadline, provider, token, context, reliability, and effect
constraints close around it.

**Narration:** “Agent frameworks tell us what may run next. They do not make capacity, time,
money, context, or irreversible actions infinite. FINITE is a framework-neutral control plane
that makes those promises executable.”

## 0:18-0:38 — IBM Bob is the development partner and first client

**Visual:** genuine Bob session history, one accepted code/test contribution, then Bob's MCP
tool list showing all 13 FINITE tools.

**Narration:** “We used IBM Bob as a core planning, coding, and verification partner. Bob is
also FINITE's first orchestration client through MCP.”

**Recording gate:** do not record this segment until real Bob evidence exists in
`docs/bob-build-log.md`. Never substitute Codex footage or a mocked Bob result.

## 0:38-1:16 — Change the plan; keep the promises

**Visual:** Physics Console nominal state, modeled provider lane reduced to one, workers reduced
to two, envelope sliders, then the two-revision recovery strip. Keep the bilingual, safety, and
publication-intent tasks visible.

**Narration:** “This fictional emergency-operations graph has eleven tasks. Reduce modeled
provider capacity or workers and the schedule changes, but the declared mandatory work remains.
Every one of these 1,080 pressure states was generated by the Python kernel and bound into this
console by SHA-256. The first capacity loss sheds optional enrichment; a second makes the
remaining promises impossible, so FINITE refuses instead of erasing settled work.”

Move the deadline and cost sliders to the known refusal state.

**Narration:** “At five seconds and five thousand micro-dollars, FINITE refuses before dispatch.
It names the binding constraint instead of spending and hoping.”

## 1:16-1:47 — Durable StormShift execution

**Visual:** run the one-command showcase. Expand the StormShift structural report and restart
events.

**Narration:** “The workload is typed, deterministic, and explicitly fictional. FINITE executes
trusted local fixtures under bounded concurrency, accounts for reported use, validates the
response plan, persists outputs, and reconstructs them after restart without recalling workers.”

**Truth card:** “Structural validation only: no semantic translation, external agency, or live
delivery claim.”

## 1:47-2:12 — Effects stop at intent

**Visual:** effect state moves to `PROPOSED`; run state reads `awaiting_effects`. Show the
separate hard-crash effect drill recovering with physical apply count one.

**Narration:** “Declared writes never enter fixture workers. They become durable, reviewable
intents. A pending write means awaiting effects, never completed. In a separate simulated-target
crash drill, the stable idempotency key prevents duplicate physical application.”

## 2:12-2:34 — Evidence, not benchmark theater

**Visual:** 450-record design, 30 paired seeds, nominal plus four transformations, per-seed
deltas and intervals; keep `descriptive-only` visible.

**Narration:** “FINITE rejects missing, changed, duplicate, or cherry-picked experiment records.
The current results are descriptive deterministic evidence. Static and sequential policies are
development references—not disguised LangGraph superiority claims.”

## 2:34-2:49 — Live IBM path

**Visual:** Bob calls `finite_preflight` and `finite_executor_drill`; then show one redacted
`live-watsonx` Granite receipt if credentials are available.

**Narration:** “Here is the real Bob-to-FINITE call. And this receipt is a bounded Granite request
through watsonx.ai, with model ID, usage, latency, and content digests.”

**Recording gate:** omit the Granite sentence and visual unless the real SDK path ran. Never show
the test-double receipt as live evidence.

## 2:49-2:58 — Close

**Visual:** social card and public repository QR/link.

**Narration:** “Agents can reason. FINITE makes them keep promises. Keep the promises. Change the
plan.”

## Capture checklist

- Use 1080p or higher; zoom UI and terminal text for mobile viewing.
- Keep a visible `SIMULATION` or `LIVE` label in every technical shot.
- Never scroll through source code when a state transition or digest can communicate the point.
- Show the public repository and demo from a signed-out browser before recording.
- Add captions and verify contrast, audio levels, URL legibility, and total duration.
- Retake any scene whose number is not reproducible from the tagged evidence bundle.
